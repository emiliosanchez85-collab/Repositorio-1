# Repositorio-1
Trabajo de introduccion 
#include <stdio.h>
int main()
{
float b,h,a;
printf("Dame los datos de la base");
scanf("%f",&b);
printf("Dame los datos de la altura");
scanf("%f",&h);
a=(b*h)/2;
printf("El resultado del area es %.2f",a);
return 0;
}
